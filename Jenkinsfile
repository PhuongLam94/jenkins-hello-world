pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'which java'
                sh 'id -un'
                sh 'java --version'
                sh '/Users/phuonglam/Downloads/apache-maven-3.5.4/bin/mvn --version'
            }
        }
    }
}